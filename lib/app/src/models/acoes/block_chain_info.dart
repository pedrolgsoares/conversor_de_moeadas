class BlockchainInfo {
  String? name;
  List<String>? format;
  double? last;
  double? buy;
  double? sell;
  double? variation;

  BlockchainInfo(
      {this.name, this.format, this.last, this.buy, this.sell, this.variation});
}